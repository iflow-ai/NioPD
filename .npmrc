# NPM Registry Configuration
registry=https://registry.npmjs.org/
@iflow-ai:registry=https://registry.npmjs.org/

# Always use exact versions
save-exact=true

# Include package-lock.json
package-lock=true

# Allow publishing to public registry
access=public